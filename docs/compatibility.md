# Compatibility

<!-- Generated by scripts/gen_docs.py -- do not edit. Run `make generate` after changing its source. -->

Where SimMirror is expected to work, and where it has been checked. A row says **verified**, with the date, only when it was checked end to end on real hardware; **expected** means it should work and nobody has checked it yet. Checked it yourself? Send a [compatibility report](https://github.com/AndrewKochulab/sim-mirror/issues/new?template=compatibility-report.yml) with `sim-mirror doctor --json`.

## macOS

SimMirror needs a Mac with a logged-in desktop session: simulators do not show over SSH or from a launch daemon.

| Version | Status | Notes |
|---|---|---|
| macOS 26 | **verified 2026-09-16** | Checked end to end on macOS 26.6.2. Xcode 26 needs macOS 26.2 or later, Xcode 27 macOS 26.6 or later. |
| macOS 15 | **expected** | With Xcode 16.4: the simctl connector only (view-only), since idb_companion 1.5 needs Xcode 26. |
| Intel Macs | **unsupported** | idb_companion 1.5 is built for Apple Silicon, and Xcode 27 runs only on Apple Silicon. |

## Xcode

| Version | Status | Notes |
|---|---|---|
| Xcode 26 | **verified 2026-09-16** | Checked end to end with Xcode 26.6 (17F113) and the iOS 18.6 and 26.5 runtimes, on an iPhone 17 Pro simulator. |
| Xcode 27 | **expected** | SimulatorKit moved to Contents/SharedFrameworks. A device booted while Device Hub is open ignores idb's input: see Troubleshooting. |
| Xcode 16.4 and earlier | **expected** | The simctl connector only (view-only). |

## Connectors

`auto` uses idb where idb_companion is installed and falls back to simctl, saying why. See Connectors.

| Connector | Status | Notes |
|---|---|---|
| idb (idb_companion 1.5) | **verified 2026-09-16** | Full control, all checked on a device: H.264 and JPEG streams, touch, drag, wheel, buttons, appearance, typing, paste and the element tree. |
| simctl | **verified 2026-09-16** | View-only, checked on a device with the companion hidden: JPEG at up to 4 frames a second, screenshots, appearance, install, launch, open URL, logs. No input and no element tree, and the tools that need them are refused. |

## MCP clients

| Client | Status | Notes |
|---|---|---|
| Claude Code in a terminal, with the plugin | **expected** | `/plugin install sim-mirror@sim-mirror`. |
| Claude Code in a terminal, with `claude mcp add` | **expected** |  |
| OpenAI Codex | **expected** | Raise `tool_timeout_sec` for build tools. |
| Cursor | **expected** |  |
| Other MCP clients over stdio | **expected** | Any client that starts a stdio server and lists its tools. |

## Browsers

H.264 is decoded with WebCodecs, which needs a secure page: http://127.0.0.1 and http://localhost qualify. Anywhere H.264 cannot be decoded, the viewer uses JPEG.

| Browser | Status | Notes |
|---|---|---|
| Chrome | **verified 2026-09-16** | Checked on Chrome: H.264 decoded in the page, a person's input, the agent cursor, an iframe and the web component. |
| Safari | **expected** |  |
| Firefox | **expected** |  |
| Edge | **expected** |  |
