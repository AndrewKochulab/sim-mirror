# SPDX-License-Identifier: Apache-2.0
"""Where SimMirror keeps things on disk: its folders (`app_support`), private files and secrets (`private`), and the
machine-wide device claims every host on the Mac shares (`claims`)."""
