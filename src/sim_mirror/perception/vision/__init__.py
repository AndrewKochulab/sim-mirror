# SPDX-License-Identifier: Apache-2.0
"""SimMirror's text reader: macOS's Vision in a small Swift helper (`text_recognizer.swift`), compiled on first use
(`platform.swift`) and run by `helper.VisionHelpers`, the `perception.ocr.TextRecognizer` a daemon reads pixels with."""
