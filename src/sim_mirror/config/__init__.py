# SPDX-License-Identifier: Apache-2.0
"""SimMirror's settings: one table (`schema`), the typed value a scope gets (`model.SimConfig`), and where values come
from -- `config.toml` (`toml_source`), the environment (`env`) and `sim-mirror config` (`writer`)."""
