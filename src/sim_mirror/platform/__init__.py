# SPDX-License-Identifier: Apache-2.0
"""The only modules that start programs on the Mac: helper processes in their own groups (`process`), Xcode's tools
through xcrun (`xcrun`), and simctl's calls (`simctl`). `scripts/check_containment.py` keeps it that way."""
