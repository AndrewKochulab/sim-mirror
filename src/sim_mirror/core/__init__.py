# SPDX-License-Identifier: Apache-2.0
"""SimMirror's core: devices brought up and let go (`manager`), streamed to viewers (`frames`, `screen_relay`), touched
by people (`screen_input`) and agents (`actions`), and composed with a host's seams in one place (`runtime`)."""
