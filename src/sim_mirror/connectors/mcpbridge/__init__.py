# SPDX-License-Identifier: Apache-2.0
"""The mcpbridge connector: Xcode 27's tools (`client`), a device's UI hierarchy read through them (`hierarchy`,
`reader`), as a connector of its own (`connector`) or merged into another's snapshots (`merge`)."""
